# Network File System

## Manager Node

``` js copy
sudo apt-get install nfs-kernel-server 
sudo apt-get install nfs-common

nano /etc/exports
/opt/exchange_data ip_worker(rw,no_subtree_check,no_root_squash,sync)

sudo exportfs -ra 
sudo service nfs-kernel-server restart
```

## Worker Node
``` js copy
sudo apt-get install nfs-common
cd /opt
sudo mkdir mount
sudo mount ipadresse_manager:/opt/exchange_data /opt/mount 
```

## Directory erstellen in /opt/exchange_data

nbgrader Kurs Directory (für Aufgaben Exchange)
``` js copy
mkdir DHBW-Kurs
```

User persistent volumen
``` js copy
mkdir jupyterhub-user-volumes
```

bwCloud Datenbank
``` js copy
mkdir Project-database
```

## Referecnces

1. [ubuntu NFS](https://wiki.ubuntuusers.de/NFS/)